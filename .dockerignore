# Don't copy these into the build context / image.
node_modules
dist
.env
.env.*
*.log
npm-debug.log*
.git
.gitignore
coverage
Dockerfile
.dockerignore
README.md
NOTES.md
test
# Observability config — bind-mounted into the prometheus/grafana containers by
# docker-compose, not needed inside the backend image.
monitoring
